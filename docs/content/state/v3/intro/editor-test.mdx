---
title: Editor Test
description: Test the Editor component
---

# Editor Test

This page tests the Editor component with react-live integration.

## Basic Example

<Editor
  code={`function HelloWorld() {
  return <div>Hello, World!</div>;
}`}
/>

## Simple Component

<Editor
  code={`function Button({ children, onClick }) {
  return (
    <button 
      onClick={onClick}
      style={{
        padding: '8px 16px',
        backgroundColor: '#3b82f6',
        color: 'white',
        border: 'none',
        borderRadius: '4px',
        cursor: 'pointer'
      }}
    >
      {children}
    </button>
  );
}`}
  hideDemo={true}
/>