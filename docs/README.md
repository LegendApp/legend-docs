# Legend site

See the [repository README](../README.md) for development, routes, contribution guidance, and the domain transition checklist.

The app uses Next.js, Fumadocs, React Native Web for interactive examples, and Bun. Run `bun dev` here for development and `bun run build` for the validated static export.
